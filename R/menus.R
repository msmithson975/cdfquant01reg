# Define the exact mapping of ftcdfq models
.model_ftcdfq <- c(
  "Arcsinh_Arcsinh_outer_W_2" = "FTCDFQ_SourceFiles/Arcsinh_Arcsinh_outer_W.R",
  "Arcsinh_Cauchy_outer_W_2" = "FTCDFQ_SourceFiles/Arcsinh_Cauchy_outer_W.R",
  "Cauchit_Arcsinh_outer_W_2" = "FTCDFQ_SourceFiles/Cauchit_Arcsinh_outer_W.R",
  "Cauchit_Cauchy_outer_W_2" = "FTCDFQ_SourceFiles/Cauchit_Cauchy_outer_W.R",
  "t2_t2_outer_W_2" = "FTCDFQ_SourceFiles/t2_t2_outer_W.R",
  "Cauchit_Arcsinh_outer_V_2" = "FTCDFQ_SourceFiles/Cauchit_Arcsinh_outer_V.R",
  "Cauchit_Cauchy_outer_V_2" = "FTCDFQ_SourceFiles/Cauchit_Cauchy_outer_V.R",
  "t2_t2_outer_V_2" = "FTCDFQ_SourceFiles/t2_t2_outer_V.R",
  "Arcsinh_Arcsinh_inner_W_2" = "FTCDFQ_SourceFiles/Arcsinh_Arcsinh_inner_W.R",
  "Arcsinh_Cauchy_inner_W_2" = "FTCDFQ_SourceFiles/Arcsinh_Cauchy_inner_W.R",
  "Cauchit_Cauchy_inner_W_2" = "FTCDFQ_SourceFiles/Cauchit_Cauchy_inner_W.R",
  "t2_t2_inner_W_2" = "FTCDFQ_SourceFiles/t2_t2_inner_W.R",
  "Arcsinh_Cauchy_inner_V_2" = "FTCDFQ_SourceFiles/Arcsinh_Cauchy_inner_V.R",
  "Cauchit_Arcsinh_inner_V_2" = "FTCDFQ_SourceFiles/Cauchit_Arcsinh_inner_V.R",
  "Cauchit_Cauchy_inner_V_2" = "FTCDFQ_SourceFiles/Cauchit_Cauchy_inner_V.R",
  "t2_t2_inner_V_2" = "FTCDFQ_SourceFiles/t2_t2_inner_V.R",
  "Arcsinh_Arcsinh_outer_W_3" = "FTCDFQ_SourceFiles/Arcsinh_Arcsinh_outer_W_3.R",
  "Arcsinh_Cauchy_outer_W_3" = "FTCDFQ_SourceFiles/Arcsinh_Cauchy_outer_W_3.R",
  "Cauchit_Arcsinh_outer_W_3" = "FTCDFQ_SourceFiles/Cauchit_Arcsinh_outer_W_3.R",
  "Cauchit_Cauchy_outer_W_3" = "FTCDFQ_SourceFiles/Cauchit_Cauchy_outer_W_3.R",
  "t2_t2_outer_W_3" = "FTCDFQ_SourceFiles/t2_t2_outer_W_3.R",
  "Cauchit_Arcsinh_outer_V_3" = "FTCDFQ_SourceFiles/Cauchit_Arcsinh_outer_V_3.R",
  "Cauchit_Cauchy_outer_V_3" = "FTCDFQ_SourceFiles/Cauchit_Cauchy_outer_V_3.R",
  "t2_t2_outer_V_3" = "FTCDFQ_SourceFiles/t2_t2_outer_V_3.R",
  "Arcsinh_Arcsinh_inner_W_3" = "FTCDFQ_SourceFiles/Arcsinh_Arcsinh_inner_W_3.R",
  "Arcsinh_Cauchy_inner_W_3" = "FTCDFQ_SourceFiles/Arcsinh_Cauchy_inner_W_3.R",
  "Cauchit_Cauchy_inner_W_3" = "FTCDFQ_SourceFiles/Cauchit_Cauchy_inner_W_3.R",
  "t2_t2_inner_W_3" = "FTCDFQ_SourceFiles/t2_t2_inner_W_3.R",
  "Arcsinh_Cauchy_inner_V_3" = "FTCDFQ_SourceFiles/Arcsinh_Cauchy_inner_V_3.R",
  "Cauchit_Arcsinh_inner_V_3" = "FTCDFQ_SourceFiles/Cauchit_Arcsinh_inner_V_3.R",
  "Cauchit_Cauchy_inner_V_3" = "FTCDFQ_SourceFiles/Cauchit_Cauchy_inner_V_3.R",
  "t2_t2_inner_V_3" = "FTCDFQ_SourceFiles/t2_t2_inner_V_3.R"
)
#
# Define the exact mapping of exftcdfq models
.model_exftcdfq <- c(
  "Ex_Arcsinh_Arcsinh_outer_W" = "FTCDFQ_SourceFiles/Ex_Arcsinh_Arcsinh_outer_W.R",
  "Ex_Arcsinh_Cauchy_outer_W" = "FTCDFQ_SourceFiles/Ex_Arcsinh_Cauchy_outer_W.R",
  "Ex_Cauchit_Arcsinh_outer_W" = "FTCDFQ_SourceFiles/Ex_Cauchit_Arcsinh_outer_W.R",
  "Ex_Cauchit_Cauchy_outer_W" = "FTCDFQ_SourceFiles/Ex_Cauchit_Cauchy_outer_W.R",
  "Ex_t2_t2_outer_W" = "FTCDFQ_SourceFiles/Ex_t2_t2_outer_W.R",
  "Ex_Cauchit_Arcsinh_outer_V" = "FTCDFQ_SourceFiles/Ex_Cauchit_Arcsinh_outer_V.R",
  "Ex_Cauchit_Cauchy_outer_V" = "FTCDFQ_SourceFiles/Ex_Cauchit_Cauchy_outer_V.R",
  "Ex_t2_t2_outer_V" = "FTCDFQ_SourceFiles/Ex_t2_t2_outer_V.R",
  "Ex_Arcsinh_Arcsinh_inner_W" = "FTCDFQ_SourceFiles/Ex_Arcsinh_Arcsinh_inner_W.R",
  "Ex_Arcsinh_Cauchy_inner_W" = "FTCDFQ_SourceFiles/Ex_Arcsinh_Cauchy_inner_W.R",
  "Ex_Cauchit_Cauchy_inner_W" = "FTCDFQ_SourceFiles/Ex_Cauchit_Cauchy_inner_W.R",
  "Ex_t2_t2_inner_W" = "FTCDFQ_SourceFiles/Ex_t2_t2_inner_W.R",
  "Ex_Arcsinh_Cauchy_inner_V" = "FTCDFQ_SourceFiles/Ex_Arcsinh_Cauchy_inner_V.R",
  "Ex_Cauchit_Arcsinh_inner_V" = "FTCDFQ_SourceFiles/Ex_Cauchit_Arcsinh_inner_V.R",
  "Ex_Cauchit_Cauchy_inner_V" = "FTCDFQ_SourceFiles/Ex_Cauchit_Cauchy_inner_V.R",
  "Ex_t2_t2_inner_V" = "FTCDFQ_SourceFiles/Ex_t2_t2_inner_V.R"
)
#
# The interactive ftcdfq selection function
#' Select a standard FTCDFQ distribution
#' @importFrom utils menu
#' @export
select_ftcdfq <- function() {
  choice_index <- menu(
    choices = names(.model_ftcdfq),
    title = "Choose a BRMS model to load into the session:"
  )
  # Process the user's choice
  if (choice_index > 0) {
    # Dynamically find the file inside the package's installed system path
    rel_path <- .model_ftcdfq[choice_index]
    # 1. Handle development mode (devtools::load_all())
    # package_file checks your raw source directory directly
    if (requireNamespace("pkgload", quietly = TRUE) && pkgload::is_dev_package("cdfquant01reg")) {
      chosen_file <- pkgload::package_file("inst", rel_path)
    } else {
      # 2. Handle normal installed mode for users
      chosen_file <- system.file(rel_path, package = "cdfquant01reg")
    }

    # 3. Source the file if found
    if (nzchar(chosen_file) && file.exists(chosen_file)) {
      # CRITICAL FIX: Explicitly attach brms so sourced files can see custom_family()
      if (!"package:brms" %in% search()) {
        attachNamespace("brms")
      }
      source(chosen_file, local = .GlobalEnv)
      message(sprintf("\n--> Successfully loaded: %s", names(.model_ftcdfq)[choice_index]))
      message("The shared and unique Stan functions are now active.")
    } else {
      warning(sprintf("Requested file could not be found. Inspected path: %s", chosen_file))
    }
  } else {
    message("Selection cancelled. No model loaded.")
  }
}
#
# The interactive selection function
#' Select an extended EXFTCDFQ distribution
#' @importFrom utils menu
#' @export
select_exftcdfq <- function() {
  choice_index <- menu(
    choices = names(.model_exftcdfq),
    title = "Choose a BRMS model to load into the session:"
  )

  # Process the user's choice
  if (choice_index > 0) {
    # Dynamically find the file inside the package's installed system path
    rel_path <- .model_exftcdfq[choice_index]
    # 1. Handle development mode (devtools::load_all())
    # package_file checks your raw source directory directly
    if (requireNamespace("pkgload", quietly = TRUE) && pkgload::is_dev_package("cdfquant01reg")) {
      chosen_file <- pkgload::package_file("inst", rel_path)
    } else {
      # 2. Handle normal installed mode for users
      chosen_file <- system.file(rel_path, package = "cdfquant01reg")
    }

    # 3. Source the file if found
    if (nzchar(chosen_file) && file.exists(chosen_file)) {
      # CRITICAL FIX: Explicitly attach brms so sourced files can see custom_family()
      if (!"package:brms" %in% search()) {
        attachNamespace("brms")
      }
      source(chosen_file, local = .GlobalEnv)
      message(sprintf("\n--> Successfully loaded: %s", names(.model_exftcdfq)[choice_index]))
      message("The shared and unique Stan functions are now active.")
    } else {
      warning(sprintf("Requested file could not be found. Inspected path: %s", chosen_file))
    }
  } else {
    message("Selection cancelled. No model loaded.")
  }
}
